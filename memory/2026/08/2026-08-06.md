# 2026-08-06

- Confirmed `gordo-labs/sovereign-apps` and
  `gordo-labs/iroh-react-native-bridge` are public GitHub repositories.
- Confirmed npm package `@gordo-labs/react-native-iroh@0.2.0` is public and
  resolves with `latest=0.2.0`.
- Restored the pre-alpha runtime after the user changed scope from implementation
  to preparation; no stabilization code remains from this session.
- Prepared source map, ordered backlog, coordinator prompt and SA-000–SA-017
  implementation prompts based on Music Hub desktop/mobile/presence behavior.
- Clarified `web-presence`: installable framework-neutral module with a Next.js
  adapter and minimal example, not a project marketing landing.
- Current branch: `codex/stabilize-sovereign-apps`; documentation changes are
  uncommitted at this point.
